@model IEnumerable<Product>

<!-- Section-->
<section >
    <div >
        <div class="row">
            <h1>Articles : </h1>
            @foreach (var product in Model)
            {
                <partial name="_ShowProduct" model="product" />
            }
        </div>
    </div>
</section>
